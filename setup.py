from setuptools import setup


setup(
    name="revin",
    version="1.0.0",
    install_requires=[],
    entry_points={
        "console_scripts": [
            "revin = revin:revin"
        ]
    }
)
